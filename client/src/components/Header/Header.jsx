import './Header.module.css';

const Header = () => {
    return (
        <nav>
            <a href="#">Ranking</a>
            <a href="#">O Nas</a>
            <a href="#">Ustawienia</a>
            <a href="#">Profil</a>
        </nav>
    );
};

export default Header;
